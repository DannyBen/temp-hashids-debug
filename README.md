HashIDs Debug Repo
==================================================

Repo created with:

    $ rails new .
    $ rails db:create
    $ rails g model user name:string
    $ rails db:migrate RAILS_ENV=test
